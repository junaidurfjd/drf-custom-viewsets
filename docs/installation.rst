============
Installation
============

At the command line::

    $ easy_install rest_framework_custom_viewsets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rest_framework_custom_viewsets
    $ pip install rest_framework_custom_viewsets
